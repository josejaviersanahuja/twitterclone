/* eslint-disable no-use-before-define */
import React, { ReactElement } from 'react'

export default function index (): ReactElement {
  return (
        <div>
            esta es una página que debería mostrar un twit
        </div>
  )
}
